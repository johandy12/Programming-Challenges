#include <iostream>

using namespace std;
int main ()
{
	float a= 1.5, b= 5, c= 7, d= 10, e, f, g;
	e= a * b;
	f= a * c;
	g= a * d;
	cout << "The number of millimeters higher than the current level that the ocean’s level will be in 5 years= " << e << endl;
	cout << "The number of millimeters higher than the current level that the ocean’s level will be in 7 years= " << f << endl;
	cout << "The number of millimeters higher than the current level that the ocean’s level will be in 10 years= " << g << endl;
	
	return 0;
}
